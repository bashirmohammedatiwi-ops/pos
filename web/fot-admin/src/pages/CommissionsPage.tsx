import { CommissionGroupsPanel } from '@/components/CommissionGroupsPanel';

/** إدارة مجاميع العمولة فقط — التقارير والصرف في قسم لاحق. */
export function CommissionsPage() {
  return <CommissionGroupsPanel />;
}
